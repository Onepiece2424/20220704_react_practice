const name = "主田";

const age = 24;

//ES2015前
// const message = "私の名前は、" + name + "です。年齢は、" + age + "です。";

// console.log(message);

//ES2015以降
const message = `私の名前は、${name}です。年齢は、${age}です。`;

console.log(message);
